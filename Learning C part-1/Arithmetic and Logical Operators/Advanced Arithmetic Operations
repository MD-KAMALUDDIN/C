#include <stdio.h>
#include <math.h>

int main() {
    int r,c;    
    double sq;
    r = 5%3;
    printf("%d\n", r);
    c = 2*2*2;
    printf("%d\n", c);
    sq = sqrt(9);
    printf("%lf", sq);
    return 0;
}
